This program will outputs ‘True’ if a given number is divisible by 3 and false when it is not divisible by 3 

Function main
Comment of the program
Declare integer num,i,temp,count
   Set temp=0
   Set count=0
print response
   " Enter the number"
get Input Number from the user
for i=0 to num/2 with increasing by one in the next iteration
     Assign temp = i * 3 
    If num == temp ,if true Then
        Assign count = 1
        Print response
            "True"
    End If
End For loop when the condition becomes false
If count == 0 ,if true Then
   print response
       "False"
End If 